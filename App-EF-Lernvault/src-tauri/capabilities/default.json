{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window (least privilege, P0).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
