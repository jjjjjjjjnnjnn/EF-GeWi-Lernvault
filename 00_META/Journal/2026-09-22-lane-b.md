---
fach: ""
thema: "Lane-B-Vergleich-Pipeline"
datum: 2026-09-22
tags: [EF, Meta]
---

# 2026-09-22 B路：Vergleich数据管线（收V4技术债）

## 改动（仅 `quizgen.ts` + `Quiz.tsx`，零新依赖）

- `VergleichItem.rubrics` 删 `belegkette/operatorabfolge`，对齐v3四维（operator/fachbegriff/beleg/vorgehen）；MOCK三条同步四维全false。
- `getVergleichItems` 真管线：vault非空→klausurrelevant笔记经 `buildDiscriminationTask(#4)` + `buildContrastTask(#5)` 生成（sourceRef恒 `notePath#行`，空抛断）；vault空才回退MOCK。新增helpers：`buildVergleichFromNote/pickDeTexts/toQuoteSegments/shortLabel/slugifyThema`（AB正确项奇偶穿插，高亮≤3）。
- `Quiz.tsx`：Vergleich评估态（`vergleichEval/vLmDegraded` + id重置effect + `toggleVergleichPill` + `evaluateVergleichLM`，同Klausur 4正则，失败启发式降级不锁死）；L2 pills改 `RUBRIC_CRITERIA` 4维可点；L3过程维绑 `vorgehenFalsch` 单切换pill。

## 验收（主Agent亲验）

- `npm run build` 两轮过（agent 1.12s / 主Agent 1.08s）；前3题逐字未动、快捷键Space/D/V/1/2+isTyping守卫未动、存储键 `eflernvault:vergleich:v1` + version:1未动、Zitierpflicht不断。
- V4两条技术债清零：MOCK-stub消除、双RUBRIC统一。
