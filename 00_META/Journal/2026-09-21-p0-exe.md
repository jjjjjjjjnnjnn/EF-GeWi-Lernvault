---
fach: ""
thema: "P0 first exe shipped"
datum: 2026-09-21
tags: [EF, Meta]
---

# 2026-09-21 P0 首个 exe 出炉

## 环境
- 用户装了 VS Community 2026（v18），自带 MSVC 14.51（`VC/Tools/MSVC/14.51.36231`），link.exe 就绪。
- Rust 1.98.1 识别 v18 无压力。

## 构建（`npx tauri build`，2分41秒）
- `ef-lernvault.exe` + NSIS `EF-Lernvault_0.1.0_x64-setup.exe`（1.8MB）+ MSI（2.7MB）。
- 启动验证：双实例窗口标题均为 `EF-Lernvault`，常驻内存各 ~25MB（对比 Electron 同级 150MB+）。
- 测试实例已 kill；安装包在本地 `src-tauri/target/release/bundle/`（gitignore，不进库）。

## 下一步
- P2：ts-fsrs 真实调度 + 到期队列（替代 mock 无限循环）。
- 用户实测：装 NSIS 包 → 顶栏"打开知识库"选 vault → 全流程走一遍。
