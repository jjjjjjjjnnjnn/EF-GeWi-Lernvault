---
fach: ""
thema: "Audit"
datum: 2026-09-21
tags: [EF, Meta]
---

# 2026-09-21 全项目审核记录

## 绿灯

- git：工作树干净，12 commits 一科一 commit，远端同步。
- INDEX：62/62 链接有效；15 篇 Journal 全挂链。
- csv：Python 精确校验 4 文件 74 数据行全部 5 列（`；` 分号格式正确）。
- WebUI：1420 报 200；电子书 342 页后台跑，conf 0.95+，fetch.log 零报错。
- 卫生：无 `data/` 泄漏；`_Downloads/` 761 件 220.7MB（全 gitignored）。

## 修了两处

1. `.gitignore`：补 `fetch.log` + `fetch.lock`（运行时文件，不应出现在 untracked）。
2. `AGENTS.md` §2：`Templates/` 脚手架模板正式豁免 frontmatter（它们是填空骨架不是笔记）。

## 误报一则（记下来防再查）

- PowerShell `Get-Content` + `-split ";"` 在无 BOM UTF-8 csv 上报 20 行坏列；
  Python 复核全对。结论：以后 csv 校验一律用 Python，PS 只看文件级。
