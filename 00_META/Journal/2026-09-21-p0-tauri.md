---
fach: ""
thema: "P0 tauri scaffold, linker blocked"
datum: 2026-09-21
tags: [EF, Meta]
---

# 2026-09-21 P0 进展：Tauri 脚手架就绪，卡 link.exe

## 已做（[App]，未推送待本轮一起）
- `src-tauri/`：Cargo.toml（tauri 2 + serde）/ build.rs / tauri.conf.json
  （de.eflernvault.app，NSIS，1280×800）/ capabilities/default.json（core:default 最小权限）
  / src/main.rs（`vault_default_path` IPC 探针 + 窗口亚系统）。
- 图标：.NET 手绘 app-icon.png（纸面+墨框+EF+靛蓝条）→ `tauri icon` 生成 6 个引用文件，
  其余（ios/android/appx/64px）已删，可再生。
- npm：+ @tauri-apps/api（runtime）+ @tauri-apps/cli（构建），0 漏洞；NOTICE 已分级记录。
- `cargo check`：crates 下载编译中，首错 `linker 'link.exe' not found`——确认只差 MSVC 链接器。

## 阻塞（需用户手动一步，我无法提权）
- 后台静默装 MSVC 失败（只落了 Installer 壳，无 workload；vswhere 无 products）。
- 用户操作：打开 `C:\Program Files (x86)\Microsoft Visual Studio\Installer\vs_installer.exe`
  → 安装/修改 **VS BuildTools 2022** → 勾选 **"使用 C++ 的桌面开发"** → 安装
  （备选：右键以管理员运行 `Temp\opencode\vs_BuildTools.exe`，同样勾 C++ workload）。
- 装完验证：`.../2022/BuildTools/VC/Tools/MSVC/*/bin/Hostx64/x64/link.exe` 存在 → 告诉我，
  我跑 `npx tauri build` 出首个 exe（约 5–15 分钟编译）。
